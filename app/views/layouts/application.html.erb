<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Sparkles</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="slack-app-id" content="A02K1J7462C">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <%= favicon_link_tag asset_path('sparkles.png') %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <div id="flash" class="col-lg-6 mx-auto">
      <% if flash.alert %>
        <div class="alert alert-danger" role="alert">
          <%= flash.alert %>
        </div>
      <% end %>

      <% if flash.notice %>
        <div class="alert alert-primary" role="alert">
          <%= flash.notice %>
        </div>
      <% end %>
    </div>

    <% if authenticated? %>
      <div class="pt-2 pe-3 text-end">
        <%= link_to sign_out_path, class: "btn btn-outline-secondary sign-out-button", data: {method: :delete} do %>
          <%= image_tag current_user.image_url, size: 25, class: "rounded", alt: current_user.name %>
          Sign out
        <% end %>
      </div>
    <% end %>

    <%= yield %>

    <footer class="mt-auto mb-2 text-center text-muted fw-bold">Made with &#10084;&#65039; by <a href="https://twitter.com/davidcelis">David Celis</a></footer>
  </body>
</html>
